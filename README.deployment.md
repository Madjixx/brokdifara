# Installation

The application is deployed in production server using `docker compose`.
This tutoral has been followed in order to deploy over HTTPS.
https://mindsers.blog/en/post/https-using-nginx-certbot-do

To renew the HTTPS certificate (each 3 months), we can use the [scripts/certrenew.sh](scripts/certrenew.sh)
